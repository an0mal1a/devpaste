[package]
name = "devpaste"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8"
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
